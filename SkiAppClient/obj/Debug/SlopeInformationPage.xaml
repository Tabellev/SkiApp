<Page
    x:Name="pageRoot"
    x:Class="SkiAppClient.SlopeInformationPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SkiAppClient"
    xmlns:common="using:SkiAppClient.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <CollectionViewSource
            x:Name="itemsViewSource"
            Source="{Binding SlopeInformation}"/>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid Margin="0,0,10,33">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="43,40,0,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="Løypeinformasjon" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,27"/>
        </Grid>
        <Image Source="Assets/Loypekart/loypekartHemsedal.PNG" HorizontalAlignment="Left" Height="608" Margin="20,10,0,0" VerticalAlignment="Top" Width="705" Grid.Row="1"/>
        <TextBlock x:Name="tbName" HorizontalAlignment="Left" Margin="761,10,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" FontSize="30" VerticalAlignment="Top" Height="60" Width="605"/>
        <TextBlock x:Name="tbNumberOfLifts" HorizontalAlignment="Left" Margin="761,93,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" FontSize="30" VerticalAlignment="Top" Height="60" Width="605"/>
        <TextBlock x:Name="tbNumberOfSlopes" HorizontalAlignment="Left" Margin="761,173,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" FontSize="30" VerticalAlignment="Top" Height="60" Width="605"/>
        <TextBlock x:Name="tbNumberOfParks" HorizontalAlignment="Left" Margin="761,253,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" FontSize="30" VerticalAlignment="Top" Height="90" Width="605"/>
        <TextBlock x:Name="tbChildrenAreas" HorizontalAlignment="Left" Margin="761,363,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" FontSize="30" VerticalAlignment="Top" Height="90" Width="605"/>
        <TextBlock x:Name="tbOtherInformation" HorizontalAlignment="Left" Margin="761,473,0,0" Grid.Row="1" TextWrapping="Wrap" Text="TextBlock" FontSize="30" VerticalAlignment="Top" Height="90" Width="605"/>
    </Grid>
</Page>

